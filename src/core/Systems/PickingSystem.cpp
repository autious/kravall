#include "PickingSystem.hpp"
#include "World.hpp"




void Core::PickingSystem::Update( float delta )
{

	/*
    for( std::vector<Entity>::iterator it = m_entities.begin();
            it != m_entities.end();
            it++ )
    {
        ExampleComponent1* ec1 = WGETC<ExampleComponent1>(*it);
        ExampleComponent2* ec2 = WGETC<ExampleComponent2>(*it);

        ec1->v += ec2->v * delta;
    }
	*/
}